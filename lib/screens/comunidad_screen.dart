import 'package:flutter/material.dart';

class ComunidadScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Comunidad")),
      body: Center(
        child: Text("Aquí irán las preguntas, foros o ayuda comunitaria"),
      ),
    );
  }
}
